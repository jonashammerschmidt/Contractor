<div class="fst-container">
    <div class="fst-header">
        <div class="fst-toolbar">
            <h1>EntityReadable: {{entity?.displayProperty}}</h1>
            <div class="divider"></div>
            <button mat-flat-button [routerLink]="['/domain-kebab/entities-kebab']"
                data-testid="domain-kebab-entities-kebab-detail-close-button">
                Schließen
            </button>
            <button mat-flat-button color="primary" (click)="onSaveClicked()"
                data-testid="domain-kebab-entities-kebab-detail-save-button">
                <mat-icon>save</mat-icon>
                Speichern
            </button>
            <button mat-flat-button color="warn" (click)="onDeleteClicked()"
                data-testid="domain-kebab-entities-kebab-detail-delete-button">
                <mat-icon>delete</mat-icon>
                Löschen
            </button>
        </div>
    </div>

    <div class="fst-content loading" *ngIf="!entity">
        <mat-progress-spinner color="primary" mode="indeterminate" diameter="50"></mat-progress-spinner>
    </div>

    <div class="fst-content" *ngIf="entity">
        <form [formGroup]="formController.formGroup" class="form-layout-container">
            <div class="form-layout-card">
            </div>
        </form>
    </div>
</div>