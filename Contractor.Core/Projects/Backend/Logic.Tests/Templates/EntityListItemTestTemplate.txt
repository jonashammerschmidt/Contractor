using ProjectName.Contract.Logic.Modules.Domain.Entities;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace ProjectName.Logic.Tests.Modules.Domain.Entities
{
    internal class EntityListItemTest : IEntityListItem
    {
        public Guid Id { get; set; }

        public static void AssertDefault(IEntityListItem entityListItem)
        {
            Assert.AreEqual(EntityTestValues.IdDefault, entityListItem.Id);
        }

        public static void AssertDefault2(IEntityListItem entityListItem)
        {
            Assert.AreEqual(EntityTestValues.IdDefault2, entityListItem.Id);
        }
    }
}