<div class="entity-kebab-update-page" *ngIf="entityUpdateForm">
    <div class="toolbar">
        <h1>Entity ändern</h1>
    </div>

    <br>
        
    <form [formGroup]="entityUpdateForm">
        <div class="buttons">
            <button mat-flat-button [mat-dialog-close]="true" color="primary" (click)="onUpdateClicked()">
                <mat-icon class="mat-icon-button-fix">save</mat-icon>
                Speichern
            </button>

            <button mat-flat-button [mat-dialog-close]="false">
                Abbrechen
            </button>
        </div>
    </form>
</div>

<div *ngIf="!entityUpdateForm">
    <mat-progress-spinner color="primary" mode="indeterminate" diameter="50"></mat-progress-spinner>
</div>